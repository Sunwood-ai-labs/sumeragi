version: '3.8'

services:
  ruri-bot:
    build: .
    container_name: ruri-discord-bot
    restart: always
    volumes:
      - ./logs:/app/logs
      - ./prompts:/app/prompts
    environment:
      - TZ=Asia/Tokyo
    env_file:
      - .env
